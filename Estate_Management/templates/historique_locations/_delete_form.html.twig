<form method="post" action="{{ path('historique_locations_delete', {'id': historique_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_location.id) }}">
    <button class="btn">Delete</button>
</form>

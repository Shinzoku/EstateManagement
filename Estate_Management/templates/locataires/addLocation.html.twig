{% extends 'base.html.twig' %}

{% block title %}Nouvelle location{% endblock %}

{% block body %}
<div class="container">
        <h2 class="text-center m-5 text-white">NOUVELLE LOCATION</h2>
        <form methode="POST" class="formBorder border border-dark p-3 mx-auto w-50">
            <div class="form-group">
                <label class="text-white" for="nom">Selectionner une location pour {{locataires.prenoms}} {{locataires.noms}}:</label>
                <select type="text" class="form-control" id="nom">
                    {% for bien in biens %}
                    <option value="{{bien.id}}">{{bien.noms}}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="text-right">
                <button type="submit" class="btn btn-outline-light mt-4">AJOUTER</button>
            </div>
        </form>
        <div class="text-center mt-5 mb-5">
            <a href="{{path('locataires_show', {'id': locataires.id})}}" class="btn btn-dark">← Retour au locataire</a>
        </div>
    </div>
{% endblock %}
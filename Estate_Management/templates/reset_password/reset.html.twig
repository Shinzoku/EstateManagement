{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <h2 class="text-center text-bold text-white m-5">Reset your password</h2>
    <div class="formBorder border border-dark p-3 w-25 mx-auto">
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <div class="text-right">
        <button class="btn btn-outline-light mt-3">Reset password</button>
        </div>
    {{ form_end(resetForm) }}
    </div>
{% endblock %}
